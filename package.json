{
  "name": "manochat-instagram-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/index.js && node --check src/lib/automation.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "4.92.0"
  }
}
